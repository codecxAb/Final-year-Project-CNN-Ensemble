# 🫁 LungCare Triage — Unified Dependencies
# For Conda environment: `lungcare`
# Install with: pip install -r requirements.txt

# ── Base AI / ML (Install PyTorch first if needed for GPU, but this handles CPU)
torch>=2.0.0

# ── Backend (FastAPI + Supported Libs)
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
sqlalchemy>=2.0.0
pydantic>=2.0.0
python-multipart>=0.0.9

# ── LLM / Agent Layer (LangGraph + Groq + Gemini API)
langchain>=0.2.0
langchain-core>=0.2.0
langchain-groq>=0.1.6
langgraph>=0.1.0
google-generativeai>=0.5.2
pypdf2>=3.0.0

# ── Frontend (Streamlit)
streamlit>=1.33.0
pandas>=2.0.0

# ── Bot (Telegram)
python-telegram-bot>=20.7

# ── Shared Utilities
requests>=2.31.0
httpx>=0.27.0
python-dotenv>=1.0.0
